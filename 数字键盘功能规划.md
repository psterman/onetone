# 数字键盘功能区域规划

## 标准数字键盘布局
```
┌───┬───┬───┬───┐
│ / │ * │ - │ ← │
├───┼───┼───┼───┤
│ 7 │ 8 │ 9 │ + │
├───┼───┼───┤   │
│ 4 │ 5 │ 6 │   │
├───┼───┼───┼───┤
│ 1 │ 2 │ 3 │   │
├───┴───┼───┤ Ent│
│   0   │ . │   │
└───────┴───┴───┘
```

## 功能区域规划

### 第一区域：Agent 与模型选择区（7-8-9）
- **7键** - Agent 切换 / 智能助手 → AG00 `switchAgent`（`/agent`）；**状态灯仍画在 AG00**
- **8键** - Claude 模型切换 → AG01 `claudeModel`（聚焦 Claude；已前台插 `/model`）
- **9键** - Codex 模式切换 → AG02 `switchModel`（`/model`）
- *已落地（默认路由）；既有用户绑定需「一键修复」或手动改才会跟新 stock*

### 第二区域：快捷操作区（4-5-6）
- **4键** - 快速执行 / 权限控制 → AG03 `permissions`（`/permissions`）
- **5键** - 常用命令 / 收藏夹 → AG04 `status`（`/status`，常用入口；完整命令菜单在 `/`）
- **6键** - 应用程序切换 / 快捷菜单 → AG05 `appsOrPlugins`（`/apps`）
- *已落地（默认路由 + Overlay 文案）*

### 第三区域：基础功能区（1-2-3）
- **1键** - 上下文切换 → ACT09 `newThread`（新建对话上下文）
- **2键** - 撤销/重做 → 软键 `UNDO` `undo`（Ctrl+Z；重做暂不占键）
- **3键** - 快速搜索 → 软键 `SEARCH` `quickSearch`（Ctrl+F）
- *已落地；发送/确认迁到 Numpad Enter（ACT12），为 3 键腾位*

### 第四区域：输入控制区（0 . / * - +）
- **0键** - 语音输入开关
- **.键** - 设置菜单
- **/键** - 命令菜单
- ***键** - 快速创建
- **-键** - 删除
- **+键** - 快捷聊天
- **Enter键** - 确认执行 → ACT12 `stopOrSend`（本刀已接物理 Enter）

### 第五区域：特殊功能键
- **←键** - 退格/取消
- **Home键**（可选）- 返回主菜单
- **End键**（可选）- 结束会话
